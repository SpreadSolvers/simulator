{
	"name": "simulator",
	"version": "0.1.0",
	"scripts": {
		"build": "napi build --release --output-dir ./dist && npm run postbuild",
		"build:debug": "napi build --output-dir ./dist && npm run postbuild",
		"postbuild": "mv dist/index.d.ts dist/simulator.node.d.ts 2>/dev/null || true"
	},
	"napi": {
		"binaryName": "simulator",
		"triples": {}
	},
	"devDependencies": {
		"@napi-rs/cli": "^3.5.1"
	}
}
